[project]
name = "jira-stories-export-script"
version = "0.0.0"
description = "Exports Jira stories to a CSV file."
readme = "https://github.com/Brown-University-Library/jira_stories_export_script/blob/main/README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx",
    "python-dotenv",
    "trio",
]

[dependency-groups]
local = []
staging = []
prod = []
